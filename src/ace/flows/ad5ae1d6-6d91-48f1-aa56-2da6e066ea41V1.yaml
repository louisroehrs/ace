tags:
  - Agent
sampleInputSchema: ""
sampleData:
  effectiveDate: string
  no1: string
description: Created by imported openApi schema
steps:
  - name: JSONATA MAP
    stepType: jsonata
    condition: ""
    config:
      maps:
        - jsonata: '{"effectiveDate":"effectiveDate"}'
          targetPath: queryParams
    description: mapping query params
  - name: REST
    stepType: rest-new
    condition: ""
    config:
      endpoint:
        url: "{{$env.DYNAMIC_APIS_URL}}/agents/{{no1}}/basicData"
        method: GET
      restRequest: JSON
      json: {}
      queryParams: "{{queryParams}}"
      headers:
        CoreSuite-DataSource-Key: "{{$headers.CoreSuite-DataSource-Key}}"
        CoreSuite-User-Name: "{{$headers.CoreSuite-User-Name}}"
      targetPath: result
    description: Retrieve Agent Details
